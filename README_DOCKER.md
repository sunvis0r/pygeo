# PyGeo - Docker Deployment Guide

## 🐳 Запуск с Docker

### Предварительные требования

- Docker Desktop или Docker Engine (версия 20.10+)
- Docker Compose (версия 2.0+)

### Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd pygeo
```

2. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env и измените пароли
```

3. **Запустите приложение:**
```bash
docker-compose up -d
```

4. **Откройте в браузере:**
```
http://localhost:8501
```

### Управление контейнерами

**Запуск:**
```bash
docker-compose up -d
```

**Остановка:**
```bash
docker-compose down
```

**Просмотр логов:**
```bash
docker-compose logs -f app
docker-compose logs -f postgres
```

**Перезапуск:**
```bash
docker-compose restart
```

**Пересборка после изменений:**
```bash
docker-compose up -d --build
```

### Структура проекта

```
pygeo/
├── app.py                      # Главное приложение Streamlit
├── Dockerfile                  # Docker образ приложения
├── docker-compose.yml          # Оркестрация контейнеров
├── requirements.txt            # Python зависимости
├── .env                        # Переменные окружения (не в git)
├── .env.example                # Пример переменных окружения
├── backend/
│   ├── __init__.py
│   ├── database.py            # Модуль работы с PostgreSQL
│   └── init_db.sql            # SQL скрипт инициализации БД
├── frontend/
│   └── modules/
│       ├── data_loader.py     # Загрузка данных из файлов
│       ├── preprocess.py      # Предобработка данных
│       └── visualizer.py      # Визуализация
└── src_data/                  # Исходные данные
    ├── WELL_*.las             # LAS файлы
    ├── dot_dtv/
    │   ├── H                  # Мощность пласта
    │   └── EFF_H              # Эффективная мощность
    └── INKL/
        └── траектории         # Траектории скважин
```

### Работа с данными

#### Источник данных

В `.env` файле можно выбрать источник данных:

```bash
# Загрузка из PostgreSQL
DATA_SOURCE=database

# Загрузка из файлов (по умолчанию)
DATA_SOURCE=files
```

#### Загрузка данных из файлов в БД

1. Запустите приложение
2. Нажмите "Загрузить все данные" в сайдбаре
3. Данные загрузятся из файлов `src_data/`
4. Автоматически сохранятся в PostgreSQL

#### Добавление новой скважины

1. Перейдите в режим "➕ Добавить скважину"
2. Заполните форму:
   - Название (например, WELL_100)
   - Координаты X, Y
   - Глубины Z1 (начало), Z2 (конец)
3. Нажмите "Добавить скважину"
4. Скважина сохранится в БД и появится на всех графиках

### База данных PostgreSQL

#### Подключение к БД

```bash
docker exec -it pygeo_postgres psql -U pygeo_user -d pygeo_db
```

#### Полезные SQL команды

```sql
-- Список всех скважин
SELECT * FROM wells;

-- Количество точек в траекториях
SELECT w.name, COUNT(t.id) as points
FROM wells w
LEFT JOIN trajectories t ON w.id = t.well_id
GROUP BY w.name;

-- Скважины с данными каротажа
SELECT w.name, COUNT(l.id) as las_points
FROM wells w
LEFT JOIN las_data l ON w.id = l.well_id
GROUP BY w.name;
```

#### Бэкап БД

```bash
# Создать бэкап
docker exec pygeo_postgres pg_dump -U pygeo_user pygeo_db > backup.sql

# Восстановить из бэкапа
docker exec -i pygeo_postgres psql -U pygeo_user pygeo_db < backup.sql
```

### Архитектура

```
┌─────────────────────────────────────────┐
│         Streamlit App (Port 8501)       │
│  ┌───────────────────────────────────┐  │
│  │  Frontend (Streamlit UI)          │  │
│  │  - Визуализация (Plotly)         │  │
│  │  - Интерактивные графики          │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  Backend (Python)                 │  │
│  │  - Data Loader (файлы)            │  │
│  │  - Database Manager (PostgreSQL)  │  │
│  │  - Preprocessor                   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│      PostgreSQL Database (Port 5432)    │
│  ┌───────────────────────────────────┐  │
│  │  Tables:                          │  │
│  │  - wells (скважины)               │  │
│  │  - trajectories (траектории)      │  │
│  │  - las_data (каротаж)             │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### Troubleshooting

**Проблема:** Таблицы не создались автоматически

PostgreSQL автоматически выполняет скрипты из `/docker-entrypoint-initdb.d/` только при **первом** запуске контейнера (когда volume пустой).

**Решение 1: Полный сброс (рекомендуется)**
```bash
# Используйте скрипт reset_db.sh
chmod +x reset_db.sh
./reset_db.sh
```

**Решение 2: Ручной сброс**
```bash
# Остановите контейнеры и удалите volumes
docker-compose down -v

# Удалите volume PostgreSQL
docker volume rm pygeo_postgres_data

# Запустите заново
docker-compose up -d

# Подождите 30 секунд для инициализации
sleep 30

# Проверьте таблицы
docker-compose exec postgres psql -U pygeo_user -d pygeo_db -c "\dt"
```

**Проверка состояния БД:**
```bash
# Используйте скрипт check_db.sh
chmod +x check_db.sh
./check_db.sh
```

**Проблема:** Контейнер не запускается
```bash
# Проверьте логи
docker-compose logs app

# Проверьте статус
docker-compose ps
```

**Проблема:** БД недоступна
```bash
# Проверьте здоровье PostgreSQL
docker-compose exec postgres pg_isready -U pygeo_user

# Перезапустите БД
docker-compose restart postgres
```

**Проблема:** Порт 8501 занят
```bash
# Измените порт в docker-compose.yml
ports:
  - "8502:8501"  # Используйте 8502 вместо 8501
```

### Разработка

**Режим разработки с hot-reload:**
```bash
# Монтируйте код как volume
docker-compose -f docker-compose.dev.yml up
```

**Запуск тестов:**
```bash
docker-compose exec app pytest
```

### Production

Для production окружения:

1. Измените пароли в `.env`
2. Используйте внешний PostgreSQL
3. Настройте SSL/TLS
4. Добавьте reverse proxy (nginx)
5. Настройте мониторинг

### Поддержка

Для вопросов и проблем создайте issue в репозитории.

---

**Версия:** 1.0  
**Дата:** 2025-12-16